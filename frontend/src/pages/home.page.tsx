import { ProfileCard } from '@/widgets/profile'

export const HomePage = () => {
	return (
		<section className='flex flex-col items-center gap-4 container mx-auto p-10'>
			<ProfileCard />
		</section>
	)
}
